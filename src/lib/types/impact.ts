export interface ServerImpact {
  gwp: {
    manufacture: number;
    use: number;
    unit: string;
  };
  pe: {
    manufacture: number;
    use: number;
    unit: string;
  };
  adp: {
    manufacture: number;
    use: number;
    unit: string;
  };
}
